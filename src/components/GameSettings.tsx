
import React from 'react';
import { UnifiedThemeSelector } from '@/components/ui/unified-theme-selector';

const GameSettings: React.FC = () => {
  return <UnifiedThemeSelector />;
};

export default GameSettings;
